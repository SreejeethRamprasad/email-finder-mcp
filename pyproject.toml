[project]
name = "email-finder-mcp"
version = "0.1.0"
description = "MCP server to find professional emails via AnyMailFinder"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=3.2.4",
    "httpx>=0.28.1",
    "python-dotenv>=1.0.1",
]
